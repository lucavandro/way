# ========================================
# CONFIGURAZIONE WAY - Esempio
# ========================================
# 
# Copia questo file come .env e compila con i tuoi dati reali
# 
# IMPORTANTE: Non committare mai il file .env nel repository!
# Il file .env contiene informazioni sensibili.

# ----------------------------------------
# CONFIGURAZIONE DATABASE
# ----------------------------------------
# Host del database MySQL (di solito localhost o un IP)
DB_HOST=localhost

# Nome del database che contiene le tabelle dell'applicazione
DB_NAME=nome_database_scuola

# Username per accedere al database
DB_USERNAME=username_database

# Password per accedere al database  
DB_PASSWORD=password_database_sicura

# ----------------------------------------
# CONFIGURAZIONE AUTENTICAZIONE
# ----------------------------------------
# Salt per l'autenticazione - DEVE essere una stringa casuale lunga
# Puoi generarla con: openssl rand -base64 64
# CAMBIA SEMPRE questo valore per ogni installazione!
AUTH_SALT=stringa_casuale_molto_lunga_e_sicura_da_generare

# ----------------------------------------
# NOTE SULLA CONFIGURAZIONE
# ----------------------------------------
#
# 1. SICUREZZA DATABASE:
#    - Usa sempre password forti
#    - Limita i privilegi dell'utente database solo alle operazioni necessarie
#    - Considera l'uso di SSL per connessioni remote
#
# 2. STRUTTURA DATABASE RICHIESTA:
#    L'applicazione si aspetta queste tabelle:
#    - Tabella orari scolastici con structure compatibile
#    - Tabella sostituzioni docenti 
#    - Tabella utenti per autenticazione
#    - Charset: utf8mb4 per supporto completo Unicode
#
# 3. DOMINI EMAIL PERMESSI:
#    L'applicazione valida email del dominio scuola
#    Modifica la funzione isEmailValid() in config.php se necessario
#
# 4. PERMESSI FILE:
#    - Il file .env deve essere leggibile dal server web
#    - Assicurati che .env NON sia accessibile pubblicamente
#    - Aggiungi .env al .gitignore per evitare di committarlo
#
# 5. TESTING:
#    Per testare la configurazione, visita i file PHP
#    Se vedi "ERRORE: Configurazione database non completata!" 
#    significa che devi ancora configurare i parametri